{
  "name": "@paws/providers",
  "version": "0.1.0",
  "private": true,
  "description": "Provider-agnostic HostProvider interface and registry for paws",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir dist --target bun",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "neverthrow": "catalog:"
  },
  "devDependencies": {
    "bun-types": "catalog:",
    "vitest": "catalog:"
  }
}
