{
  "name": "@paws/scheduler",
  "version": "0.1.0",
  "private": true,
  "description": "Least-loaded worker selection for paws fleet scheduling",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir dist --target bun",
    "test": "vitest run",
    "typecheck": "tsc --build"
  },
  "dependencies": {
    "@paws/domain-fleet": "workspace:*"
  },
  "devDependencies": {
    "vitest": "^4.1.2"
  }
}
