{
  "name": "@paws/runtime",
  "version": "0.1.0",
  "private": true,
  "description": "Runtime adapter interface — pluggable execution backends for paws sessions",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "bun build ./src/index.ts --outdir dist --target bun",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "neverthrow": "^8.2.0"
  },
  "devDependencies": {
    "vitest": "^4.1.2"
  }
}
