{
  "name": "@paws/domain-daemon",
  "version": "0.1.0",
  "private": true,
  "description": "Daemon domain — Daemon, Trigger, DaemonStore, routes",
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@hono/zod-openapi": "catalog:",
    "@paws/domain-agent": "workspace:*",
    "@paws/domain-common": "workspace:*",
    "@paws/domain-network": "workspace:*",
    "@paws/domain-policy": "workspace:*",
    "@paws/domain-session": "workspace:*",
    "zod": "catalog:"
  },
  "devDependencies": {
    "vitest": "catalog:"
  }
}
