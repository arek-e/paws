---
title: paws
description: Protected Agent Workspace Sandboxes — zero-trust credential injection for AI agents.
template: splash
hero:
  tagline: Zero-trust credential injection for AI agents. Secrets never enter the sandbox.
  actions:
    - text: Get Started
      link: /getting-started/install/
      icon: right-arrow
      variant: primary
    - text: View on GitHub
      link: https://github.com/arek-e/paws
      icon: external
      variant: minimal
---

import { Card, CardGrid, Code } from '@astrojs/starlight/components';

<Code code="curl -fsSL https://getpaws.dev/install.sh | bash" lang="bash" title="Install paws" />

<CardGrid>
  <Card title="Zero secrets in the VM" icon="approve-check">
    API keys never enter the sandbox. A per-VM TLS proxy injects credentials at the network layer.
    If compromised, there's nothing to steal.
  </Card>
  <Card title="Sub-second boot" icon="rocket">
    Firecracker memory snapshots restore VMs in under a second. Each session gets a fresh, ephemeral
    microVM.
  </Card>
  <Card title="Port exposure" icon="external">
    Agents run fullstack apps, users access them via Pangolin tunnel URLs with per-port access
    control: SSO, PIN, or email whitelist.
  </Card>
  <Card title="Agent harness" icon="puzzle">
    Claude Code pre-installed. Provide a prompt and constraints, the workload is auto-generated.
    More frameworks coming.
  </Card>
</CardGrid>

## How it works

```
Agent in VM: curl https://api.anthropic.com/v1/messages
  │
  ├─ VM resolves api.anthropic.com (real IP)
  ├─ iptables DNAT rewrites destination → per-VM proxy
  ├─ Proxy terminates TLS, reads SNI
  │
  ├─ Domain in allowlist?
  │   ├─ YES: inject x-api-key header, forward to real API
  │   └─ NO:  drop connection (TCP RST)
  │
  └─ Agent sees normal HTTPS response
     Never saw the API key. Not in env, not in memory, not on disk.
```
