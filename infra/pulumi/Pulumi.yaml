name: paws
runtime:
  name: nodejs
  options:
    typescript: true
description: >
  paws cluster provisioning — provisions Hetzner Cloud servers, bootstraps
  kubeadm, and deploys the paws K8s manifests in one pass.
