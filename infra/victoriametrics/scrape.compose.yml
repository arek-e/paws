# Prometheus scrape config for docker-compose deployment.
# VictoriaMetrics uses this to collect metrics from the gateway.

scrape_configs:
  - job_name: paws-control-plane
    scrape_interval: 15s
    static_configs:
      - targets:
          - gateway:4000
        labels:
          instance: control-plane
